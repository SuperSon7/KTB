spring:
  datasource:
    url: jdbc:mysql://localhost:3306/practice?serverTimezone=UTC&useSSL=false
    username: root
    password: zsed1235
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

  logging:
    level:
      org.hibernate.SQL: debug
      org.hibernate.type.descriptor.sql: trace